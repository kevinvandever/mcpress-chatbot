[phases.setup]
# Install Node.js in a Python project
cmds = ["apt-get update", "apt-get install -y nodejs npm tesseract-ocr"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "cd frontend && npm install && npm run build"
]

[start]
cmd = "python start_railway_fullstack.py"